//index.html 을 위한 js file
function login(username, password) {
    if(username){
        console.log("login message " + username + " " + password);
    }
    
};

function logout() {
    console.log('logout');
}
